# Web framework
flask>=3.0
flask-login>=0.6

# Auth
bcrypt>=4.0

# HTTP / scraping
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
urllib3>=2.0

# Data
pandas>=2.0
python-dotenv>=1.0
scipy>=1.11          # statistical modeling for estimate_middling_ev.py

# CLI
click>=8.1

# Production server
gunicorn>=21.2

# Database (prod)
psycopg2-binary>=2.9

# Timezone data — Python's zoneinfo module (used throughout app.py for
# kickoff times, day-of-week grouping, lock status) has no built-in
# timezone database on Windows, unlike Linux/Mac. Without this,
# ZoneInfo("America/New_York") raises ZoneInfoNotFoundError.
tzdata>=2024.1
